description: |
    An example sensor that reads the GPIO level defined in input-gpios.

compatible: "aosong,dht"

properties:
    dio-gpios:
        type: phandle-array
        required: true
        description: |
            Input gpios.

include: base.yaml